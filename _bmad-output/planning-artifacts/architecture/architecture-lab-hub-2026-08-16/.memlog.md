---
scope: lab-hub static site (single page, FR/EN)
purpose: spine des invariants pour refonte contenu+cartes+motion
altitude: feature
updated: 2026-08-16T14:20
---

- (constraint) AD-1 [ADOPTED]: statique pur, zero runtime reseau (hors liens externes). Le JS ne fait que render + decor.
- (constraint) AD-2 [ADOPTED]: projects.json = source unique de verite projet, etendue d'un champ notes {fr,en}; alimente par lab-infra/audit-repos.py au build, jamais a la volée.
- (constraint) AD-3 [ASSUMPTION]: dates de push GitHub ingerees au build (script refresh) et mises en cache dans projects.json (champ last_push); si API indispo -> date absente + badge 'no signal', la carte ne casse pas.
- (constraint) AD-4 [ADOPTED]: aucune nouvelle dependance runtime hors gsap deja present; monogramme = SVG deterministe genere cote client depuis le hash du nom (pas d'assets).
- (constraint) AD-5 [ASSUMPTION]: motion via gsap (ScrollTrigger pour reveal), strictement decoratif, gate par prefers-reduced-motion; jamais source d'info.
- (constraint) AD-6 [ADOPTED]: i18n: toute copy dans src/i18n.ts; notes bilingues dans projects.json; zero em/en dash.
- (constraint) AD-7 [ADOPTED]: lab-hub est un lecteur seul (read-only) des donnees; aucune ecriture, aucun backend.
- (event) Workspace architecture lie, invariants loggues
